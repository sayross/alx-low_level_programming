#include "main.h"
#include <stdio.h>

/**
 * print_array - prints n elements of an array of integers
 * @a: array
 * @n: n elements
 * Return: Void
 */

void print_array(int *a, int n)
{
	int i;

	printf(s[0]);
	for (i = 1; i <= n - 1; i++)
	{
		printf(", %d", *(s + i));
	}
	printf("\n");
}
