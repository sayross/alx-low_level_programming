#include "main.h"

/**
 * puts2 - check the code
 * @str: string
 * Return: void
 */

void puts2(char *str)
{
	int i;

	while (*(str + i) != '\0')
	{
		if (i % 2 == 0)
		{
			_putchar(*(s + i));
		}
		i++;
	}
}

